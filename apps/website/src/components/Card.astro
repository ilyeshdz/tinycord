---
const { as = "div", class: className = "" } = Astro.props;
const classes = [
    "border border-default bg-muted p-5 hover:border-[#2dd4bf]",
    className,
]
    .filter(Boolean)
    .join(" ");
const Tag = as;
---

<Tag class={classes}>
    <slot />
</Tag>
